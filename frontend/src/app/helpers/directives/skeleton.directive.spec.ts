import { SkeletonDirective } from './skeleton.directive';
import { Component, TemplateRef, ViewChild } from '@angular/core';
import { TestBed } from '@angular/core/testing';

describe('SkeletonDirective', () => {


  it('should create an instance', () => {
    // const directive = new SkeletonDirective('my-class', '');
    // expect(directive).toBeTruthy();
  });
});

// describe('MyComponent', () => {
//   let component: MyComponent;

//   beforeEach(() => {
//     TestBed.configureTestingModule({
//       declarations: [MyComponent],
//     });

//     const fixture = TestBed.createComponent(MyComponent);
//     component = fixture.componentInstance;
//     fixture.detectChanges();
//   });

//   it('should do something with the template', () => {
//     // Create a mock TemplateRef object using EmbeddedViewRef
//     const mockTemplateRef = {
//       createEmbeddedView: () => {},
//     } as TemplateRef<any>;

//     // Call the doSomethingWithTemplate function with the mock template
//     component.doSomethingWithTemplate(mockTemplateRef);

//     // Add any additional test expectations here
//   });
// });
